
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Tasks {
  id String @id @default(dbgenerated("gen_random_uuid()"))
  task String
  done Boolean
}
